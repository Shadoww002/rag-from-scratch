Baseline results:
  Q1 — correct (partial)
  Q2 — WRONG  (BERT answer was circular)
  Q3 — correct (partial)
  Q4 — correct
  Q5 — WRONG  (vocabulary mismatch — "introduced" vs "proposed")

  Accuracy:  2.5/5
  Avg score: 0.467

  # Save this — you'll compare every improvement against it

BASELINE = {
    "pipeline":          "hybrid + reranker",
    "chunk_size":        300,
    "overlap":           30,
    "retrieval":         "BM25 + Dense RRF",
    "reranker":          "cross-encoder/ms-marco-MiniLM-L-6-v2",
    "faithfulness":      0.40,
    "answer_relevance":  0.80,
    "context_precision": 0.88,
    "context_recall":    0.78,
}